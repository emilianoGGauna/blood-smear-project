# Database backend: sqlite or postgres
PROPERTY_DB_BACKEND=sqlite

# Used when sqlite backend is selected
PROPERTY_SQLITE_PATH=backend/data/properties.db

# Used when postgres backend is selected
# Example: postgresql://user:password@localhost:5432/properties
PROPERTY_POSTGRES_DSN=
